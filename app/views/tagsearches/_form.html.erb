  <div class="search_form ml-4">
    <%= form_with url: tagsearches_search_path, method: :get, local: true do |f| %>
      <%= f.text_field :content %>
      <%= f.submit "タグ検索" %>
    <% end %>
  </div>
